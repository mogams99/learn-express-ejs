<%- include('partials/header.ejs') %>
    <%- include('partials/navbar.ejs') %>

    <h1>Random Number: <%= random %></h1>
    <% if (random % 2 === 0) { %>
        <h3>Bilangan Genap</h3>
    <% } else { %>
        <h3>Bilangan Ganjil</h3>
    <% } %>
    <h3>That number is: <%= random%2 === 0 ? 'even' : 'odd' %></h3>
    
<%- include('partials/footer.ejs') %>
